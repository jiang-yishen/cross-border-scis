import paramiko

client = paramiko.SSHClient()
client.set_missing_host_key_policy(paramiko.AutoAddPolicy())
client.connect('47.239.173.51', username='root', password='JWYjys5210@', timeout=30)

# 在服务器上追加文档内容
phase_doc = r'''

---

## Q024: Phase 1 前端UI优化（全局样式+导航栏升级）

**优化内容：**

| 优化项 | 修改前 | 修改后 | 技术方案 |
|--------|--------|--------|----------|
| 侧边栏宽度 | 默认窄边栏（180px） | 加宽至225-230px | CSS: `[data-testid="stSidebar"] {min/max-width}` |
| 导航按钮样式 | 默认Radio单选按钮 | 大卡片样式（渐变背景+左侧竖条+悬停动效） | CSS: 自定义按钮样式 + 选中态渐变 |
| 导航分组 | 无分组 | 核心运营/物流调度/系统管理 三大分组 | 分组标题居中+两侧装饰线 |
| Logo区域 | 无品牌展示 | 深蓝渐变卡片+品牌名+版本号 | HTML+CSS渐变卡片 |
| 页面标题 | 默认st.title() | 白色卡片+左侧4px竖条+大图标 | st.markdown()自定义HTML |

**CSS选择器策略演进：**
- 最初使用 `.nav-card-wrapper .stButton > button` 父子选择器 → Streamlit组件独立渲染导致不生效
- 最终改为 `[data-testid="stSidebar"] button` 直接选择器 → 成功生效

---

## Q025: Phase 2 首页Dashboard+数据可视化升级

**优化内容：**

| 优化项 | 技术方案 |
|--------|----------|
| KPI卡片 | 新增左侧4px彩色竖条+图标背景+阴影悬停效果 |
| 图表容器 | 统一圆角12px+阴影+悬停放大 |
| Plotly主题 | 统一字体/边距/网格线/图例位置 |
| 快速指标区 | 新增快捷操作入口（预测趋势/库存预警/调拨建议） |

---

## Q026: Phase 3 需求预测+补货计划页面升级

**优化内容：**

| 页面 | 升级项 |
|------|--------|
| 需求预测分析 | 标题卡片+预测趋势图+MAPE分布图+TOP10 SKU排名表 |
| 补货计划看板 | 标题卡片+补货量对比柱状图+SKU明细表格+状态标签 |

---

## Q027: Phase 4 库存监控+调拨建议+采购物流页面升级

**优化内容：**

| 页面 | 升级项 |
|------|--------|
| 库存健康监控 | 标题卡片+ABC-XYZ散点图+库龄分布图+预警表格 |
| 调拨建议 | 标题卡片+调拨优先级柱状图+调拨矩阵热力图+建议明细 |
| 采购物流跟踪 | 标题卡片+物流状态漏斗图+在途订单饼图+到货跟踪表格 |

---

## Q028: Phase 5 使用指南+运维中心页面升级

**优化内容：**

| 页面 | 升级项 |
|------|--------|
| 使用指南 | 标题卡片+操作手册下载按钮+快速导航卡片+系统亮点展示 |
| 运维中心 | 管理员登录区+问题反馈表单+系统维护模块（工单管理+通知发布） |

---

## Q029: Phase 6 登录验证系统（含权限过滤）

**核心功能：**

| 功能 | 实现方案 |
|------|----------|
| 登录页面 | 左侧蓝白渐变装饰区+右侧登录表单+试玩账号提示 |
| 用户权限 | 3个预置角色：admin（全权限）/ planner（无运维）/ viewer（仅首页+指南） |
| 权限过滤 | 基于ROLE_PERMISSIONS字典，在components.py导航循环中过滤页面 |
| 退出登录 | 导航栏用户卡片+"🚪 退出"按钮，一键清除session_state并刷新 |
| 安全设计 | 未登录时CSS隐藏侧边栏，强制显示登录页 |

**技术难点：**
1. 第一次修改时误操作导致 components.py 缩进错误 → 使用sed精准修复
2. 第二次修改时从git恢复导致Phase 1-5修改丢失 → 从本地server_backup恢复
3. 权限过滤时`group_pages`变量作用域问题 → 统一在循环顶部定义过滤变量

---

## Q030: Phase 7 性能测试与优化

**优化内容：**

| 优化项 | 技术方案 | 预期效果 |
|--------|----------|----------|
| 数据缓存 | `st.cache_data(ttl=300)` 包装所有 `load_*()` 函数 | 减少重复文件读取，提升页面切换速度 |
| 首页KPI缓存 | `st.cache_data(ttl=60)` 缓存 `compute_kpis()` | 频繁访问首页时减少计算量 |
| 并发压测 | Locust脚本模拟50用户并发访问 | 评估系统承载能力，生成压测报告 |

**压测脚本配置：**
```python
# 压测命令
locust -f locustfile.py --host=http://47.239.173.51:8501 -u 50 -r 10 -t 60s --headless

# 参数说明
-u 50: 50并发用户
-r 10: 每秒新增10用户
-t 60s: 持续60秒
--headless: 无Web界面，直接输出结果
```

**压测场景权重：**
- 首页访问（权重3）：最频繁操作
- 预测页/库存页（权重2）：中频操作
- 补货页/物流页（权重1）：低频操作

'''

# 追加到项目问题记录文档
sftp = client.open_sftp()
with sftp.file('/opt/scis/项目问题记录文档.md', 'a') as f:
    f.write(phase_doc.encode('utf-8'))
print("OK: 项目问题记录文档已更新（Q024-Q030）")

client.close()
